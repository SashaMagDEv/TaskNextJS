export interface News {
    id: number;
    title: string;
    thumbnail: string;
    date: string;
    short_description: string;
    likes: number;
}

export interface Category {
    id: number;
    name: string;
}
